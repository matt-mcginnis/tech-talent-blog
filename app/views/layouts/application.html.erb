<!DOCTYPE html>
<html>
    <head>
        <title>TechTalentBlog</title>
        <%= csrf_meta_tags %>
        <%= csp_meta_tag %>

        <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    </head>

    <body>

        <nav class="navbar navbar-light">
            <%= link_to "FallBlog", root_path, class: "navbar-brand" %>
            <form class="form-inline my-2 my-lg-0">
                <input class="form-control mr-sm-2" style="background-color: #F7E9D4;" type="search" placeholder="Search..." aria-label="Search">
                <button class="btn btn-primary my-2 my-sm-0" type="submit">Search</button>
            </form>
        <div class="right">
            <% if user_signed_in? %>
                <!-- <%= gravatar_tag current_user.email, size: 50 %> -->
                <%= image_tag current_user.avatar.url, size: 50, class: "img-circle" %>
                <%= link_to "Edit Profile", edit_user_registration_path, class: "btn btn-primary" %>
                <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-primary" %>
            <% end %>
        </div>
        </nav>

        <%= yield %>
    </body>
</html>
