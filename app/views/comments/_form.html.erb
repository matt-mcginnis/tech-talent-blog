<%= form_with(model: comment, local: true) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label for="usr"><%= form.label :author %></label>
    <%= form.text_field :author, class: "form-control", id: "usr" %>
  </div>

  <div class="form-group">
    <label for="comment"><%= form.label :comment_entry %></label>
    <%= form.text_area :comment_entry, class: "form-control", rows: 5, id: "comment" %>
  </div>

  <div class="field">
      <%= form.hidden_field :post_id, value: @post.id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
